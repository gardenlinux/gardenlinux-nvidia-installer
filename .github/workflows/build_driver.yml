name: Build Drivers (non-container artifacts)
# This workflow is retired. Driver compilation is now performed inside the
# build_image.yml workflow: both open and proprietary tarballs are compiled
# and embedded directly into the container image.
#
# This file is kept as a no-op to avoid breaking any external references.

on:
  workflow_call:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is retired. Driver compilation now happens inside build_image.yml."
