name: Build and Publish Container Images (matrix)

on:
  workflow_call:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

permissions:
  id-token: write
  packages: write

jobs:
  release-branch:
     runs-on: ubuntu-latest
     outputs:
       release_branch: ${{ steps.get-release-branch.outputs.release_branch }}
       image_folder: ${{ steps.get-release-branch.outputs.image_folder }}
     steps:
      - name: Get release branch
        id: get-release-branch
        run: |
          # Get the latest release branch, based on the latest release tag
          TAG=$(gh release list --repo "${{ github.repository }}" --json tagName \
              --jq '[.[] | select(.tagName | test("^[0-9]+\\.[0-9]+\\.[0-9]+$"))] | first | .tagName')
          MAJOR_VERSION=$(echo "$TAG" | cut -d'.' -f1)
          RELEASE_BRANCH="releases/${MAJOR_VERSION}.x.x"
          echo "release_branch=${RELEASE_BRANCH}" >> $GITHUB_OUTPUT
          MAJOR=$(echo $TAG | cut -d. -f1)
          MINOR=$(echo $TAG | cut -d. -f2)
          PATCH=$(echo $TAG | cut -d. -f3)
          if [[ $PATCH -gt 0 ]]; then
            OLD_TAG="${MAJOR}.$MINOR.$((PATCH-1))"
          elif [[ $MINOR -gt 0 ]]; then
            PREV_MINOR=$((MINOR-1))
            OLD_TAG=$(gh release list --repo "${{ github.repository }}" --json tagName \
              --jq --arg major "$MAJOR" --arg minor "$PREV_MINOR" \
              '[.[] | select(.tagName | test("^" + $major + "\\." + $minor + "\\.[0-9]+$"))] | sort_by(.tagName | split(".") | map(tonumber)) | last | .tagName')
          fi
          echo "image_folder_old=/${OLD_TAG}" >> $GITHUB_OUTPUT
          echo "image_folder=$(echo "/${TAG}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  generate-matrix:
    runs-on: ubuntu-latest
    needs: release-branch
    outputs:
      build_matrix: ${{ steps.set-matrix.outputs.build_matrix }}
      manifest_matrix: ${{ steps.set-matrix.outputs.manifest_matrix }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.release-branch.outputs.release_branch }}
          
      - name: Install PyYAML and jq
        run: |
          pip install pyyaml
          sudo apt-get install -y jq

      - name: Generate build and manifest matrices
        id: set-matrix
        run: |
          MATRIX_JSON=$(python3 .ci/generate_matrix.py)
          echo "build_matrix=$(echo "$MATRIX_JSON" | jq -c '.build')" >> $GITHUB_OUTPUT
          echo "manifest_matrix=$(echo "$MATRIX_JSON" | jq -c '.manifest')" >> $GITHUB_OUTPUT

  build:
    needs: [generate-matrix, release-branch]
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-matrix.outputs.build_matrix) }}
    env:
      GL_VERSION: ${{ matrix.os_version }}
      DRIVER_VERSION: ${{ matrix.driver_version }}
      KERNEL_FLAVOR: ${{ matrix.kernel_flavour }}
      TARGET_ARCH: ${{ matrix.arch }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.release-branch.outputs.release_branch }}

      - name: Lowercase github.repository
        run: echo "FOLDER_NAME=${GITHUB_REPOSITORY@L}" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: build_image
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          IMAGE_PATH="${{ env.REGISTRY }}/${{ env.FOLDER_NAME }}${{ needs.release-branch.outputs.image_folder }}/driver"
          OLD_IMAGE_PATH="${{ env.REGISTRY }}/${{ env.FOLDER_NAME }}"${{ needs.release-branch.outputs.image_folder_old }}/driver"
    
          TAG="$DRIVER_VERSION-${{ steps.extract_kernel.outputs.kernel_name }}-gardenlinux0"
          DRIVER_MAJOR_VERS="${DRIVER_MAJOR_VERS%%.*}"
          TAG1="$DRIVER_MAJOR_VERS-${{ steps.extract_kernel.outputs.kernel_name }}-gardenlinux0"
    
          # Check if current image exists
          if docker manifest inspect "$OLD_IMAGE_PATH:$TAG" 2>/dev/null; then
            echo "Image $OLD_IMAGE_PATH:$TAG already exists, skipping build"
            echo "Pull from old image path and retag to new image path"
            docker pull "$OLD_IMAGE_PATH:$TAG"
            docker tag "$OLD_IMAGE_PATH:$TAG" "$IMAGE_PATH:$TAG"
            docker push "$IMAGE_PATH:$TAG"
            docker tag "$OLD_IMAGE_PATH:$TAG1" "$IMAGE_PATH:$TAG1"
            docker push "$IMAGE_PATH:$TAG1"
          else
            make build \
              WORKSPACE_DIR="${{ github.workspace }}" \
              GL_VERSION=$GL_VERSION \
              DRIVER_VERSION=$DRIVER_VERSION \
              KERNEL_FLAVOR=$KERNEL_FLAVOR \
              TARGET_ARCH=$TARGET_ARCH \
              IMAGE_PATH=${IMAGE_PATH}

            TAG1=$(cat "${{ github.workspace }}/tag1")
            TAG2=$(cat "${{ github.workspace }}/tag2")

            docker push $IMAGE_PATH:$TAG1
            docker push $IMAGE_PATH:$TAG2
          fi

  manifest:
    needs: [build, generate-matrix]
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.generate-matrix.outputs.manifest_matrix) }}
    env:
      IMAGE_NAME: "driver"
      GL_VERSION: ${{ matrix.os_version }}
      DRIVER_VERSION: ${{ matrix.driver_version }}
      KERNEL_FLAVOR: ${{ matrix.kernel_flavour }}
      TARGET_ARCH: amd64

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.release-branch.outputs.release_branch }}

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase github.repository
        run: echo "FOLDER_NAME=${GITHUB_REPOSITORY@L}" >> $GITHUB_ENV

      - name: Create and push multi-arch manifest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          KERNEL_NAME=$(make extract-kernel-name \
            TARGET_ARCH=$TARGET_ARCH \
            GL_VERSION=$GL_VERSION \
            KERNEL_FLAVOR=$KERNEL_FLAVOR)

          KERNEL_VERSION="${KERNEL_NAME%-*}"
          DRIVER_MAJOR_VERS="${{ matrix.driver_version }}"
          DRIVER_MAJOR_VERS="${DRIVER_MAJOR_VERS%%.*}"
          IMAGE_REGISTRY_FOLDER="${{ env.REGISTRY }}/${{ env.FOLDER_NAME }}${{ needs.release-branch.outputs.image_folder }}"
          MANIFEST_TAG="${IMAGE_REGISTRY_FOLDER}/${{ env.IMAGE_NAME }}:${DRIVER_MAJOR_VERS}-${KERNEL_VERSION}-gardenlinux0"
          AMD64_IMAGE="${IMAGE_REGISTRY_FOLDER}/${{ env.IMAGE_NAME }}:${DRIVER_MAJOR_VERS}-${KERNEL_VERSION}-amd64-gardenlinux0"
          #ARM64_IMAGE="${IMAGE_REGISTRY_FOLDER}/${{ env.IMAGE_NAME }}:${DRIVER_MAJOR_VERS}-${KERNEL_VERSION}-arm64-gardenlinux0"

          docker manifest create $MANIFEST_TAG $AMD64_IMAGE $ARM64_IMAGE
          docker manifest push $MANIFEST_TAG
